import { RGBColor } from "colorthief";

export type musicType = {
  artist: string;
  name: string;
  text: string;
  image: string;
};

export type colorType = {
  array: RGBColor;
  rgb: string;
};
